import React from "react";
import FormImg from "./FormImg";

export default function Form({ className }){
    return(
        <section>
            <div>
                    <FormImg />
            </div>
        </section>
    )
}